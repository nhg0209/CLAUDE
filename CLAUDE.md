# CLAUDE.md

이 저장소는 **Obsidian 볼트**입니다. **오프로드 주행(off-road autonomous driving)** 논문을 읽으며 정리한 노트를 담습니다.

브랜치는 `OFFROAD`입니다.

## 학습 목표 (문맥)

이 볼트는 **연구 주제 탐색 / 서베이** 단계입니다.
특정 플랫폼을 재현하는 것이 목표가 아니라, **분야의 지도를 먼저 그리는 것**이 목표입니다.

> 오프로드 주행에서 무엇이 열린 문제이고, 어느 계보가 어떤 질문에 답해 왔는지를 파악한다.
> 그 위에서 "내가 붙을 자리"를 정한다.

따라서 **논문 하나하나의 완결성보다 계보와 대조가 우선**입니다.
새 논문을 읽으면 반드시 `MOC/오프로드 지도.md`의 해당 축에 링크를 추가하고,
**기존 논문 중 무엇을 반박하거나 이어받는지**를 「🔗 연결」에 적습니다.

### 세 축

이 볼트가 다루는 범위입니다. 이 밖의 것(E2E 주행, navigation foundation model 등)은
미해결 링크로만 남기고 파고들지 않습니다.

| 축 | 질문 |
|---|---|
| **A. Traversability / 지형 인지** | 어디를 밟아도 되는가. 그 라벨은 누가 주는가 |
| **B. Planning · Control** | 예측이 불확실할 때 무엇을 최적화하는가 |
| **C. RL / Sim-to-Real** | 시뮬에 없는 지형 물리를 어떻게 메우는가 |

### 용어 규칙
**전문 용어는 영문 원문 그대로**, 설명만 한국어로.
(`traversability`를 "주행가능성"으로 번역하면 논문·코드와 매칭이 안 되기 때문)

## ⚠️ RL 볼트와의 관계 — 위키링크가 넘어가지 않는다

같은 GitHub 저장소(`nhg0209/CLAUDE`)의 **다른 브랜치 `RL`에 별도 볼트**가 있습니다.
Obsidian에서는 **두 개의 vault로 따로 열리므로 `[[위키링크]]` 가 서로 넘어가지 않습니다.**

- 이 볼트 안: 평소대로 `[[BADGR (Kahn 2020)]]`
- **RL 볼트를 가리킬 때: 위키링크 금지.** 반드시 아래 형식의 평문으로 씁니다

```
→ RL 볼트 「프로젝트 스택」 §8-5
→ RL 볼트 「RL 지도」 · Sim-to-Real 절
```

위키링크로 쓰면 이 볼트에 **빈 노드가 생겨** 그래프가 오염됩니다.
반대로 두 볼트 모두에서 자주 쓰이는 개념(예: `Domain Randomization`)은
**각 볼트에 각자의 노트를 둡니다.** 중복을 허용하되, 관점을 다르게 씁니다.

## 저장소의 역할

사용자는 로컬 PC의 Obsidian에서 이 볼트를 엽니다. Claude는 원격 세션에서 동작하므로
로컬 파일에 직접 접근할 수 없고, **git을 통해서만** 노트를 전달합니다.

```
Claude: 정리 → .md 작성 → commit → push (OFFROAD)
사용자: git pull (Obsidian Git 플러그인이 자동 수행)
```

## 디렉터리

| 경로 | 용도 |
|---|---|
| `MOC/` | Map of Content — 지도 노트. 계보와 색인 |
| `Papers/` | 논문 한 편당 노트 하나 |
| `Concepts/` | 개념 노트. 여러 논문이 공유하는 개념을 독립 노트로 분리 |
| `Questions/` | 질문 로그 색인 |
| `Templates/` | 노트 템플릿 |

## 작성 규칙

### 파일명
- 논문: `Papers/BADGR (Kahn 2020).md` — 통칭 + 제1저자 + 연도
- 개념: `Concepts/Traversability.md` — 논문에서 부르는 이름 그대로
- 띄어쓰기 허용. Obsidian 위키링크가 공백을 처리합니다.

### 문법 — Obsidian 전용 기능을 적극 사용
- **위키링크** `[[BADGR (Kahn 2020)]]` — 마크다운 링크(`[]()`)보다 우선. 단 외부 URL은 마크다운 링크
- **수식** 인라인 `$\mu$`, 블록 `$$...$$` — MathJax
- **콜아웃** `> [!note]`, `> [!warning]`, `> [!question]`, `> [!tip]`, `> [!abstract]`, `> [!danger]`
- **mermaid** ` ```mermaid ` 코드펜스 — 계보도, 파이프라인 흐름
- **YAML frontmatter** — Dataview 쿼리를 위해 반드시 포함

### 아직 안 읽은 논문은 미해결 링크로 남긴다
`[[EVORA (Cai 2023)]]`처럼 링크만 걸어두면 그래프 뷰에서 빈 노드로 보입니다.
**다음에 읽을 것이 자동으로 드러나므로**, 존재하지 않는 노트로 링크하는 것을 피하지 않습니다.

### 개념 노트를 적극 분리한다
두 편 이상의 논문에서 같은 개념이 나오면 `Concepts/`로 독립시키고 양쪽에서 링크합니다.
이 분야에서 특히 그럴 값어치가 있는 것: `Traversability`, `Slip`, `CVaR`,
`Self-Supervised Labeling`, `Elevation Map`, `Terramechanics`, `Privileged Learning`.

### 확인하지 않은 것은 비워둔다
RL 볼트의 원칙을 그대로 따릅니다.

> ✅ = 논문 원문에서 직접 확인 · ⚠️추정 = 근거는 있으나 미검증 · ❓ = 확인 못 함

**arXiv 번호·연도·저자를 기억으로 채우지 않습니다.** 확인했으면 ✅, 아니면 비워둡니다.

## 논문 노트 구조

`Templates/논문 정리 템플릿.md`를 따릅니다.
이 분야는 **시스템 논문이 많아** RL 볼트의 템플릿과 강조점이 다릅니다.

1. **좌표** — 어느 축인가, 무엇을 이어받고 무엇을 뒤집었나
2. **문제 정의** — 기존 방법이 *구체적으로 어떤 지형에서* 깨지는지
3. **핵심 아이디어** — 한 문단
4. **★ supervision — 무엇이 라벨인가** — 이 분야 논문을 가르는 축
5. **방법** — 파이프라인 + 입출력 형태
6. **실험 조건** — 플랫폼·센서·지형·속도·주행거리. **이게 없으면 논문 간 비교가 불가능**
7. **결과의 해석** — 저자 주장 / 실제 근거 / 약한 부분 분리
8. **질문 로그**
9. **연결**

## 설명의 원칙

사용자는 논문을 읽으며 질문합니다. 답할 때:

- **자기지도 논문은 라벨 생성기가 본체다.** 네트워크 구조를 설명하기 전에
  **supervision signal이 어디서 오는지**를 먼저 짚습니다. 대부분의 기여가 거기 있습니다.
- **"어디서 굴렸는가"를 항상 같이 말한다.** 모래사장에서 3 m/s와 숲에서 10 m/s는 다른 문제입니다.
  성능 숫자만 옮기면 논문을 잘못 읽습니다.
- **직관 + 수식 + 실패 모드**를 함께 준다. "안 하면 무엇이 망가지는가"가 가장 잘 남습니다.
- **저자 주장과 실험 근거를 구분**한다. 필드 로보틱스는 특히 데모 영상과 정량 결과의 간극이 큽니다.
- 사용자는 **RL·자율주행 레이싱 배경**이 있습니다. 아는 개념(SAC, domain randomization,
  asymmetric actor-critic, Frenet frame, MPC)에 붙여 설명하면 훨씬 빨리 붙습니다.
  단 **위키링크가 아니라 평문으로** RL 볼트를 참조합니다 (위의 ⚠️ 절).
- 질문에 답한 내용은 해당 논문 노트의 「❓ 질문 로그」와 `Questions/질문 로그.md`에 기록합니다.

## 커밋

- 논문 하나 정리 = 커밋 하나 (`Papers/BADGR 정리 추가`)
- 질문 답변 기록 = 별도 커밋 가능
- 푸시는 항상 `git push -u origin OFFROAD`
