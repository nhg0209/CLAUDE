# CLAUDE.md

이 저장소는 **Obsidian 볼트**입니다. RL(강화학습) 논문을 읽으며 정리한 노트를 담습니다.

## 학습 목표 (문맥)

사용자의 RL 학습은 **목적 지향적**입니다. 일반 교양이 아니라 다음을 위한 것:

> RL 사전지식 0에서 출발해, TM07 리포트
> *"Reinforcement Learning End-to-End Autonomous Racing Architecture with Asymmetric SAC and Sim-to-Real Pipeline"*
> 수준의 스택을 직접 재현·개선한다.

상세 로드맵은 `docs/RL_ROADMAP.md`에 있습니다. **논문을 설명할 때 항상 이 목표와 연결**하세요.
"이 개념이 TM07의 어느 구성요소에 해당하는가"를 짚어주면 훨씬 잘 붙습니다.

우선순위 논문: SAC(1801.01290) → SAC v2(1812.05905) → Asymmetric Actor-Critic(1710.06542) → TM07

### 용어 규칙
로드맵의 방침을 따릅니다 — **RL 용어는 영문 원문 그대로**, 설명만 한국어로.
(`policy`를 "정책"으로 번역하면 논문·코드와 매칭이 안 되기 때문)

## 저장소의 역할

사용자는 로컬 PC의 Obsidian에서 이 볼트를 엽니다. Claude는 원격 세션에서 동작하므로
로컬 파일에 직접 접근할 수 없고, **git을 통해서만** 노트를 전달합니다.

```
Claude: 정리 → .md 작성 → commit → push
사용자: git pull (Obsidian Git 플러그인이 자동 수행)
```

작업 브랜치는 `RL`입니다.

## 디렉터리

| 경로 | 용도 |
|---|---|
| `MOC/` | Map of Content — 지도 노트. 계보와 색인 |
| `Papers/` | 논문 한 편당 노트 하나 |
| `Concepts/` | 개념 노트. 여러 논문이 공유하는 개념을 독립 노트로 분리 |
| `Questions/` | 질문 로그 색인 |
| `Templates/` | 노트 템플릿 |
| `docs/` | 기존 문서. `RL_ROADMAP.md` — 사용자가 직접 작성. **함부로 수정하지 말 것** |

## 작성 규칙

### 파일명
- 논문: `Papers/PPO (Schulman 2017).md` — 통칭 + 저자 + 연도
- 개념: `Concepts/Advantage Function.md` — 논문에서 부르는 이름 그대로
- 띄어쓰기 허용. Obsidian 위키링크가 공백을 처리합니다.

### 문법 — Obsidian 전용 기능을 적극 사용
- **위키링크** `[[PPO (Schulman 2017)]]` — 마크다운 링크(`[]()`)보다 우선
- **수식** 인라인 `$\gamma$`, 블록 `$$...$$` — MathJax
- **콜아웃** `> [!note]`, `> [!warning]`, `> [!question]`, `> [!tip]`, `> [!abstract]`
- **mermaid** ` ```mermaid ` 코드펜스 — 계보도, 알고리즘 흐름
- **YAML frontmatter** — Dataview 쿼리를 위해 반드시 포함

### 아직 안 읽은 논문은 미해결 링크로 남긴다
`[[TRPO (Schulman 2015)]]`처럼 링크만 걸어두면 그래프 뷰에서 빈 노드로 보입니다.
**다음에 읽을 것이 자동으로 드러나므로**, 존재하지 않는 노트로 링크하는 것을 피하지 않습니다.

### 개념 노트를 적극 분리한다
두 편 이상의 논문에서 같은 개념이 나오면 `Concepts/`로 독립시키고 양쪽에서 링크합니다.
이렇게 해야 백링크가 쌓이면서 "이 개념을 다룬 논문 전부"가 자동으로 모입니다.

## 논문 노트 구조

`Templates/논문 정리 템플릿.md`를 따릅니다. 핵심 섹션:

1. **기호 정리** — 그 논문만의 표기를 [[기호 사전]]과 대조
2. **문제 정의** — 기존 방법이 *구체적으로 어디서* 깨지는지
3. **핵심 아이디어** — 수식 없이 한 문단
4. **수식 유도** — 각 변형이 *왜 정당한지*까지
5. **알고리즘** — 의사코드 + 텐서 shape
6. **실험** — 저자 주장 / 실제 근거 / 약한 부분을 분리
7. **질문 로그** — 세션에서 나온 질문과 답
8. **연결** — 선행·후속·개념·대조군

## 설명의 원칙

사용자는 논문을 읽으며 질문합니다. 답할 때:

- **수식은 기호 하나하나 의미를 푼다.** 특히 기댓값이 *어떤 분포*에 대한 것인지.
- **직관 + 수식 + 실패 모드**를 함께 준다. "안 하면 무엇이 망가지는가"가 가장 잘 남는다.
- **저자 주장과 실험 근거를 구분**한다. 논문이 주장하는 것과 실험이 보여주는 것은 자주 다르다.
- 질문에 답한 내용은 해당 논문 노트의 「❓ 질문 로그」와 `Questions/질문 로그.md`에 기록한다.

## 커밋

- 논문 하나 정리 = 커밋 하나 (`Papers/PPO 정리 추가`)
- 질문 답변 기록 = 별도 커밋 가능
- 푸시는 항상 `git push -u origin RL`
