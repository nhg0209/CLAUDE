# CLAUDE.md

이 저장소는 **Obsidian 볼트**입니다. **오프로드 주행(off-road autonomous driving)** 논문을 읽으며 정리한 노트를 담습니다.

브랜치는 `OFFROAD`입니다.

## 이 볼트가 담는 것 — 딱 세 가지

| | 어디에 |
|---|---|
| **논문별 내용** | `Papers/<논문>.md` — 한 편당 노트 하나 |
| **용어** | `MOC/용어 사전.md` — 분야 표준 용어·기호 |
| **파생 질문** | `Questions/질문 로그.md` — 읽으며 나온 질문과 답 |

`MOC/논문 목록.md`는 **색인일 뿐**입니다 — 읽은 논문 표와, 서지만 확인해 둔 후보 목록.

> [!important] 하지 않는 것
> **계보 정리, 분야 조망, 읽기 순서 제안, 논문 간 대조, 볼트 간 접점 분석.**
> 논문 사이의 관계를 굳이 그리지 않습니다. **각 논문을 그 자체로** 정리합니다.
>
> 사용자가 **명시적으로 요청할 때만** 위와 같은 메타 정리를 씁니다.

## ⚠️ 읽지 않은 논문에 대해서는 쓰지 않는다 ★

**이 볼트에서 가장 강한 규칙입니다.**

> 논문의 내용은 **세션에서 사용자와 함께 읽은 뒤에만** 채웁니다.
> 초록이나 검색 결과만 보고 요약을 쓰지 않습니다.

- `MOC/논문 목록.md`의 후보 목록에는 **서지 정보만** 둡니다 (제목·저자·연도·arXiv·학회)
- 한 줄 요약, 기여 정리, supervision 방식은 **읽은 뒤에** 씁니다

> [!danger] 이 규칙이 있는 이유
> 검색 결과 기반 요약은 **그럴듯한데 틀린 곳을 짚어낼 수가 없습니다.**
> 빈칸은 정보가 없다는 사실을 정직하게 보여주지만, 틀린 요약은 그것도 감춥니다.

### 새 논문을 다룰 때의 순서

1. 사용자가 논문을 지정 → 함께 읽으며 질문·답변
2. 그 내용으로 `Papers/<논문>.md` 작성 (템플릿 따름)
3. 그 논문에서 처음 나온 **분야 표준 용어**는 `MOC/용어 사전.md`에도 추가
4. 질문은 노트의 「❓ 질문」과 `Questions/질문 로그.md` 양쪽에 기록
5. `MOC/논문 목록.md`의 후보 목록에서 해당 줄을 지움 (Dataview 표로 올라가므로)

### 용어 규칙
**전문 용어는 영문 원문 그대로**, 설명만 한국어로.
(`traversability`를 "주행가능성"으로 번역하면 논문·코드와 매칭이 안 되기 때문)

## RL 볼트

같은 저장소의 `RL` 브랜치에 RL 논문 볼트가 따로 있습니다.
**지금은 두 볼트를 잇지 않습니다.** 노트에 RL 볼트를 참조하지 않습니다.

사용자가 아는 개념(SAC, domain randomization, MPC 등)에 빗대어 **대화에서 설명**하는 것은
괜찮습니다. 그걸 **노트에 적는 것**이 다릅니다.

## 저장소의 역할

사용자는 로컬 PC의 Obsidian에서 이 볼트를 엽니다. Claude는 원격 세션에서 동작하므로
로컬 파일에 직접 접근할 수 없고, **git을 통해서만** 노트를 전달합니다.

```
Claude: 정리 → .md 작성 → commit → push (OFFROAD)
사용자: git pull (Obsidian Git 플러그인이 자동 수행)
```

## 디렉터리

| 경로 | 용도 |
|---|---|
| `Papers/` | 논문 한 편당 노트 하나 |
| `MOC/` | `논문 목록.md` (색인) · `용어 사전.md` |
| `Questions/` | 질문 로그 |
| `Templates/` | 노트 템플릿 |

## 작성 규칙

### 파일명
- 논문: `Papers/BADGR (Kahn 2020).md` — 통칭 + 제1저자 + 연도
- 띄어쓰기 허용. Obsidian 위키링크가 공백을 처리합니다.

### 문법 — Obsidian 전용 기능을 적극 사용
- **수식** 인라인 `$\mu$`, 블록 `$$...$$` — MathJax
- **콜아웃** `> [!note]`, `> [!warning]`, `> [!question]`, `> [!tip]`, `> [!abstract]`, `> [!danger]`
- **mermaid** ` ```mermaid ` 코드펜스 — **한 논문 안의 파이프라인 흐름**에만. 계보도는 그리지 않습니다
- **YAML frontmatter** — Dataview 쿼리를 위해 반드시 포함
- **위키링크** `[[용어 사전]]` — **볼트 안에서는 적극 씁니다**. 아래 별도 절 참고

### 위키링크

**볼트 안에서는 적극 씁니다.** 어디에 무엇을 거는지:

| 어디서 | 무엇으로 |
|---|---|
| 논문 노트 | [[용어 사전]], [[질문 로그]], [[논문 목록]] |
| `논문 목록`의 후보 표 | 각 논문 (아직 노트가 없으면 **미해결 링크**로 남음) |
| `용어 사전` | 그 용어가 실제로 나온 **읽은 논문** |
| `질문 로그` | 그 질문이 나온 논문 |
| 논문 ↔ 논문 | 필요하면 겁니다. 단 **링크만 걸고 계보 서술은 하지 않습니다** |

- **미해결 링크**(노트가 없는 대상)를 피하지 않습니다. 그래프 뷰의 **빈 노드 = 아직 안 읽은 논문**이고,
  클릭하면 그 이름으로 노트가 만들어집니다
- 외부 URL은 마크다운 링크 `[제목](url)`
- **RL 볼트로는 위키링크가 넘어가지 않습니다.** 애초에 참조하지 않습니다 (위 「RL 볼트」 절)

### 확인하지 않은 것은 비워둔다

> ✅ = 논문 원문에서 직접 확인 · ⚠️추정 = 근거는 있으나 미검증 · ❓ = 확인 못 함

**arXiv 번호·연도·저자를 기억으로 채우지 않습니다.** 확인했으면 ✅, 아니면 비워둡니다.

## 논문을 받았을 때의 작업 절차 ★

사용자가 **직접 읽은** 논문을 첨부합니다. 그러면:

### 1. 읽는다

PDF는 `Read` 도구로 **전부** 읽습니다. 초록·검색 결과로 쓰지 않습니다.

PDF 처리 도구가 필요하면 (컨테이너가 세션마다 초기화되므로 매번):
```bash
pip install PyMuPDF        # 텍스트·이미지 추출, 페이지 렌더링
```

### 2. `Papers/<통칭> (제1저자 연도).md` 작성

템플릿을 따르되 **논문에 없는 내용은 쓰지 않습니다.**

> [!important] 핵심 위주로, 사족 없이
> - 분야 배경 설명, 다른 논문과의 비교, "이게 왜 중요한가" 같은 **군더더기를 붙이지 않습니다**
> - 목적은 **사용자가 다음에 다시 읽을 때 빠르게 내용을 되찾는 것**입니다
> - 논문에 없어서 모르는 것은 ❓로 비워둡니다

- **수식은 논문의 기호 그대로** 옮기고 MathJax로 렌더링합니다.
  **기호 정의를 빠뜨리지 않습니다** — 각 기호가 무엇이고 어떤 차원인지
- 논문의 **절 번호·식 번호·Figure 번호**를 그대로 인용합니다. 나중에 원문을 찾을 수 있어야 합니다
- 실험 조건(플랫폼·센서·지형·속도·데이터셋)은 **숫자를 그대로** 옮깁니다

### 3. Figure를 적극 활용한다

핵심 그림은 **이미지로 추출해 노트에 임베드**합니다.

| | |
|---|---|
| 저장 위치 | `Papers/attachments/<논문 폴더명>/fig<N>.png` |
| 임베드 | `![[fig3.png]]` |
| 바로 아래에 | 원문 캡션 + **이 그림이 무엇을 보여주는지 한두 줄** |

- 그림 번호는 **논문 원문 그대로** (`Fig. 3`)
- 벡터 그림은 해당 영역을 렌더링해서 자릅니다 (임베드된 래스터 이미지가 없을 수 있음)
- 추출이 안 되면 최소한 **"Fig. 3 — 무엇을 보여주는가"** 를 텍스트로 남깁니다
- 표(Table)도 중요하면 마크다운 표로 옮깁니다

### 4. 질문에 답한다

사용자가 노트를 다시 읽고 질문합니다.

> [!danger] 추측하지 않는다
> - **논문에 있는 내용으로만** 답하고, **어디에 있는지**(절·식 번호·Figure)를 같이 밝힙니다
> - 논문에 없으면 **"논문에 없다"고 말합니다.** 일반 지식으로 메울 때는
>   **그것이 논문 밖 내용임을 명시**합니다
> - 기억으로 숫자·저자·연도를 채우지 않습니다

답한 내용은 **양쪽에** 기록합니다:
- 해당 논문 노트의 「❓ 질문」
- `Questions/질문 로그.md` (개념별 색인 표 + 시간순 로그)

### 5. 커밋

논문 하나 정리 = 커밋 하나. 질문 기록은 별도 커밋.

---

## 논문 노트 구조

`Templates/논문 정리 템플릿.md`를 따릅니다.

1. **문제 정의** — 기존 방법이 *구체적으로 어떤 지형에서* 깨지는지
2. **핵심 아이디어** — 한 문단
3. **★ supervision — 무엇이 라벨인가**
4. **방법** — 파이프라인 + 입출력 형태
5. **실험 조건** — 플랫폼·센서·지형·속도·주행거리. **이게 없으면 숫자를 옮길 수 없다**
6. **결과의 해석** — 저자 주장 / 실제 근거 / 약한 부분 분리
7. **한계**
8. **📖 용어** — 이 논문이 처음 쓰거나 다른 뜻으로 쓰는 것
9. **❓ 질문**

## 설명의 원칙

사용자는 논문을 읽으며 질문합니다. 답할 때:

- **자기지도 논문은 라벨 생성기가 본체다.** 네트워크 구조를 설명하기 전에
  **supervision signal이 어디서 오는지**를 먼저 짚습니다.
- **"어디서 굴렸는가"를 항상 같이 말한다.** 모래사장에서 3 m/s와 숲에서 10 m/s는 다른 문제입니다.
  성능 숫자만 옮기면 논문을 잘못 읽습니다.
- **직관 + 수식 + 실패 모드**를 함께 준다. "안 하면 무엇이 망가지는가"가 가장 잘 남습니다.
- **저자 주장과 실험 근거를 구분**한다. 필드 로보틱스는 데모 영상과 정량 결과의 간극이 큽니다.
- 사용자는 **RL·자율주행 레이싱 배경**이 있습니다. 아는 개념(SAC, domain randomization,
  asymmetric actor-critic, Frenet frame, MPC)에 빗대어 **설명**하면 빨리 붙습니다.
  단 그 비유를 **노트에 계보로 적지는 않습니다.**
- 질문에 답한 내용은 해당 논문 노트의 「❓ 질문」과 `Questions/질문 로그.md`에 기록합니다.

## 커밋

- 논문 하나 정리 = 커밋 하나 (`Papers/BADGR 정리 추가`)
- 질문 답변 기록 = 별도 커밋 가능
- 푸시는 항상 `git push -u origin OFFROAD`
